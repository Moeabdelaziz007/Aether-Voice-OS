[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.ruff.format]
# Enable formatting.
quote-style = "double"
indent-style = "space"

[tool.isort]
profile = "black"
line_length = 120
